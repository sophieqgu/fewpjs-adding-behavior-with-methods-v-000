// Your code here
class Cat {
  constructor(name, gender) {
    this.name = name;
    this.gender = gender
  }
  speak: () => `${this.name} says meow!`;
}

class Dog {
  speak: (name) => `${name} says woof!`;
}

class Bird {
  speak: (name) => {
    if (this.)
  }`${name} says squawk!`;
}
