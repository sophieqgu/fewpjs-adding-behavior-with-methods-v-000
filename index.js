// Your code here
class Cat {
  speak: (name) => `${name} says meow!`;
}

class Dog {
  speak: (name) => `${name} says woof!`;
}

class Bird {
  speak: (name) => {
    if (this.)
  }`${name} says squawk!`;
}
